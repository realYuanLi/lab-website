{
  "name": "huang-lab-website",
  "version": "1.0.0",
  "private": true,
  "description": "Huang Lab — Penn Medicine. Static multi-page site.",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
